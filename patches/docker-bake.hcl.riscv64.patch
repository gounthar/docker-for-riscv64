--- docker-bake.hcl
+++ docker-bake.hcl
@@ -222,6 +222,11 @@
   no-cache-filter = ["run"]
   output = ["${DESTDIR}"]
 }
+
+group "riscv64" {
+  targets = ["docker-image"]
+  platforms = ["linux/riscv64"]
+}
