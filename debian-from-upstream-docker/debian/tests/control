Tests: basic-smoke
Depends: debian-archive-keyring, debootstrap, docker.io, docker-cli
Restrictions: allow-stderr, needs-root, needs-internet, isolation-machine, flaky

#TODO: find out why this hangs. Is @builddeps@ too long?
#Tests: integration
#Depends: ca-certificates, curl, git, golang-check.v1-dev, jq, @, @builddeps@
#Restrictions: allow-stderr, isolation-machine, needs-root, rw-build-tree, flaky
